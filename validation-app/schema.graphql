type Query {
   greeting:String
}

type Mutation {
  # use the input type SignUpInput to reduce the number of parameters in signUp function
   signUp(input:SignUpInput):String
}

input SignUpInput {
   email:String!,
   password:String!,
   firstName:String!
}